[project]
name = "protokflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "JongSeonKim", email = "jongseon9312@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
protokflow = "protokflow:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "mypy==1.10.0",
    "pytest==9.1.1",
]
