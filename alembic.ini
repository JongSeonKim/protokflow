[alembic]
script_location = %(here)s/backend/database/migrations
prepend_sys_path = .
# The connection URL is injected at runtime by backend.database.migrate or the
# PROTOKFLOW_DATABASE_URL environment variable; it is never stored here.
